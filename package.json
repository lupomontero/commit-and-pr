{
  "name": "commit-and-pr",
  "version": "1.0.2",
  "main": "index.js",
  "bin": {
    "commit-and-pr": "./cli.js"
  },
  "scripts": {
    "pretest": "eslint .",
    "test": "jest --verbose --coverage",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^6.0.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.18.0",
    "jest": "^24.8.0"
  }
}
